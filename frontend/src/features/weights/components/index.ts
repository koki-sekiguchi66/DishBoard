/**
 * weights/components バレルエクスポート（Phase 3 更新）
 *
 * Phase 3 変更:
 *   - WeightChart を除去（features/analysis/ に移行済み）
 *   - WeightForm を .tsx に変更
 */
export { default as WeightForm } from "./WeightForm";
