/**
 * auth feature バレルエクスポート（Phase 4 TS版）
 *
 * Login.jsx / Register.jsx → .tsx 化に伴い index も .ts に変更。
 */
export { default as Login } from "./components/Login";
export { default as Register } from "./components/Register";
